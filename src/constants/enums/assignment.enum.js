/**
 * Assignment Enums
 */

const ASSIGNMENT_TYPE = {
  ESSAY: "essay",
  RECORDING: "recording",
  FILE_UPLOAD: "file_upload"
};

const ASSIGNMENT_TYPE_LIST = Object.values(ASSIGNMENT_TYPE);

module.exports = {
  ASSIGNMENT_TYPE,
  ASSIGNMENT_TYPE_LIST
};
